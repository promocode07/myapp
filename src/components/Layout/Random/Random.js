import React from "react"
import './Random.scss'

const Random = (props) => {
    return(
        <div className="popup">
          <h3>{props.title}</h3>  
        </div>
    )
}

export default Random